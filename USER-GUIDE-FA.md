# 👤 راهنمای کاربر عادی — بدون دانش فنی (ویندوز + گوشی)

این راهنما برای کسی است که **داکر بلد نیست و با کد کار نمی‌کند**.

> ⚠️ یک واقعیت مهم درباره گوشی: **گوشی نمی‌تواند این برنامه را اجرا کند**
> (گوشی داکر ندارد). پس همیشه یک کامپیوتر/سرور برنامه را اجرا می‌کند و
> گوشی فقط با مرورگر به آن وصل می‌شود — مثل اینستاگرام که روی گوشی است
> ولی سرورش جای دیگری است.

---

## بخش اول — روشن کردن روی کامپیوتر ویندوز (فقط بار اول)

### قدم ۱ — نصب Docker Desktop (حدود ۵ دقیقه)

۱. این صفحه را باز کنید: https://www.docker.com/products/docker-desktop/
۲. دکمه **Download for Windows** را بزنید و نصب کنید.
۳. ویندوز را در صورت درخواست Restart کنید.
۴. برنامه **Docker Desktop** را باز کنید و صبر کنید تا پایین صفحه سبز و نوشته شود **Engine running**.

> اگر موقع نصب پرسید «WSL 2» آن را قبول کنید (پیش‌نیاز داکر در ویندوز است).

### قدم ۲ — دانلود دو فایل

از همین ریپو این دو فایل را دانلود کنید (دکمه `...` هر فایل → `Download`):

- `Start-ReceiptVision.bat` — روشن کردن برنامه
- `Stop-ReceiptVision.bat` — خاموش کردن برنامه

هر دو را در یک پوشه (مثلاً دسکتاپ) بگذارید.

### قدم ۳ — روشن کردن

۱. روی **`Start-ReceiptVision.bat` دابل‌کلیک** کنید. یک پنجره مشکی باز می‌شود و خودش همه کارها را می‌کند.
۲. صبر کنید تا بنویسد **[OK] برنامه آماده است** — مرورگر خودش باز می‌شود روی:
   **http://localhost:8080/**
۳. دکمه **ساخت حساب** را بزنید (یک نام کاربری و رمز دلخواه) و وارد شوید.

## بخش دوم — استفاده با گوشی 📱

### روش ۱ — گوشی و کامپیوتر به یک وای‌فای وصل‌اند (بدون هزینه، همین امروز)

۱. روی کامپیوتر، در پنجره مشکی (cmd) این را بزنید تا آدرس کامپیوتر را ببینید:
   `ipconfig` — دنبال `IPv4 Address` بگردید، مثلاً `192.168.1.50`
۲. اگر ویندوز پرسید، اجازه دسترسی شبکه (Firewall) را بدهید.
۳. روی گوشی (به همان وای‌فای وصل باشد) مرورگر را باز کنید و بزنید:
   **http://192.168.1.50:8080** (به‌جای `192.168.1.50` همان عدد خودتان)
۴. وارد حساب خودتان شوید. دکمه **📷 عکس گرفتن** مستقیم دوربین گوشی را باز می‌کند.
۵. برای نصب مثل اپ: در کروم گوشی منوی `⋮` → **Add to Home Screen** (در آیفون: دکمه Share → **Add to Home Screen**). از بعد آیکون ReceiptVision روی گوشی است و تمام‌صفحه باز می‌شود.

> بیرون از خانه (اینترنت موبایل) این روش کار **نمی‌کند** — برای آن روش ۲ لازم است.

### روش ۲ — دسترسی از همه‌جا (اینترنت) — نیاز به سرور و دامنه دارد

اگر می‌خواهید با اینترنت موبایل هم کار کند، برنامه باید روی یک سرور همیشه‌روشن با آدرس اینترنتی (دامنه) و HTTPS اجرا شود. این کار یک‌بار توسط یک آشنا/متخصص انجام می‌شود:

```bash
DOMAIN=receipt.example.com APP_JWT_SECRET="$(openssl rand -base64 48)" \
  docker compose -f docker-compose.prod.yml up --build -d
```

بعد گوشی فقط می‌زند `https://receipt.example.com` — بدون هیچ نصب دیگری.

## خاموش کردن (کامپیوتر)

- روی **`Stop-ReceiptVision.bat` دابل‌کلیک** کنید.
- ⚠️ نگران نباشید: رسیدها و حساب شما **پاک نمی‌شود** و دفعه بعد برمی‌گردد.

## سوالات پرتکرار

| مشکل | راه‌حل ساده |
|---|---|
| می‌گوید «داکر پیدا نشد» | بخش اول قدم ۱ را کامل کنید |
| صفحه روی گوشی باز نمی‌شود (وای‌فای) | هر دو به یک وای‌فای باشند؛ فایروال ویندوز اجازه بدهد؛ IP درست باشد |
| دکمه دوربین باز نمی‌شود | به مرورگر اجازه دوربین بدهید؛ بیرون از HTTPS فقط روی `localhost` و وای‌فای خانگی کار می‌کند |
| رمز را فراموش کردم | به‌خاطر حریم خصوصی راه برگردانی نیست؛ حساب جدید بسازید |
| کامپیوتر عوض کردم | رسیدها روی همان دستگاه قبلی است (منتقل نمی‌شود) |

## حریم خصوصی شما

- هر رسید فقط با حساب **خودتان** دیده می‌شود؛ هیچ کاربر دیگری به آن دسترسی ندارد.
- در روش خانگی، اطلاعات روی **کامپیوتر خودتان** است نه اینترنت.
- در روش سروری، اطلاعات روی آن سرور است — سرور باید مال خودتان/مطمئن باشد.
